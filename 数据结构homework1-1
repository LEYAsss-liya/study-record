#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int a[10];
    //初始化随机数种子
    srand((unsigned)time(NULL));
    for(int i = 0; i<10; i++){
        //rand() % 100 随机生成0~99
        a[i] = rand() % 100 + 1;
    }
    //冒泡排序
    for(int i=0; i<9; i++){
            //注意越界和条件(i<n-1 j=0开启 j<n-1-i)
        for(int j=0sss; j<9-i; j++)
        if(a[j] > a[j+1]){
            int temp = a[j];
            a[j] = a[j+1];
            a[j+1] = temp;
        }
    }
    for(int i=0; i<10; i++){
        printf("%d ", a[i]);
    }


    return 0;
}
